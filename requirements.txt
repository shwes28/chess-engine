# Chess engine dependencies

# PyTorch CPU-only (lighter build for deployment)
--index-url https://download.pytorch.org/whl/cpu
torch

# Core
python-chess
numpy>=1.24.0
flask>=3.0.0
tqdm>=4.65.0
gunicorn>=21.0.0
